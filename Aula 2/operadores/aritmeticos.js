// Aritméticos
let a = 10;
let b = 5;
let sum = a + b; // soma
let difference = a - b; // subtração
let product = a * b; // multiplicação
let quotient = a / b; // divisão
let remainder = a % b; // modulo (resto da divisão) Ex.: 10/3=3 e resta 1
let exponentiation = a ** b; // exponenciação
// duas formas de executar:

console.log("soma: " + sum); // puxando a variável "sum"
console.log("difference: " + (a - b)); // elimino a necessidade de ter a variável
//resrto da divisão
console.log("resto: " + remainder);
